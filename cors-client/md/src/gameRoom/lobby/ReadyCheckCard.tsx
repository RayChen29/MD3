// import {useEffect,useState} from 'react';

export default function ReadyCheck(){
    //TODO: Make readyToggle be either ready x/y, Start, or 3,2,1 countdown
    //TODO: cancelready emergency cancel if you end up not ready to start.



    return(
        // <span className="readyToggle"> {"Ready x/x" || 'Start'}</span>
        <span className="cancelReady"> Cancel</span>
    )
}